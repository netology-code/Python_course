import config
import game_math
import application.employee.reports
import application.employee.reports

print("Secret password: {}".format(config.password))
print(game_math.calculate_distance(1, 1, 2, 2))
print(application.employee.reports.config)
